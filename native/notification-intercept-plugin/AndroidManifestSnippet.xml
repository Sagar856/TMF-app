<!--
  Reference snippet inserted into android/app/src/main/AndroidManifest.xml by
  scripts/patch-android.mjs during the CI build. Kept here so the required
  permission/service declarations are version-controlled and reviewable.
-->

<!-- Placed as a direct child of <manifest> -->
<uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

<!-- Placed as a direct child of <application> -->
<service
    android:name="com.trackmoneyflow.app.TmfNotificationListenerService"
    android:label="Track Money Flow Transaction Interceptor"
    android:exported="true"
    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
  <intent-filter>
    <action android:name="android.service.notification.NotificationListenerService" />
  </intent-filter>
</service>

